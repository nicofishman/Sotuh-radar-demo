---
import { Sections } from '../sections';

import SidebarGenerator from './Sidebar/SidebarGenerator';
---

<div
	class="bg-navbar bg-card-gradient fixed right-0 top-0 h-screen max-w-[400px] w-1/3 md:block hidden"
>
	{Sections.map((s) => <SidebarGenerator section={s} />)}
</div>
